{
  "name": "mapApp",
  "version": "1.0.0",
  "description": "mapApp",
  "scripts": {
    "postinstall": "bower install",
    "start": "http-server ./app -a localhost -p 3000 -c-1 -o"
  },
  "license": "ISC",
  "devDependencies": {
    "bower": "^1.7.7",
    "http-server": "^0.9.0"
  }
}
